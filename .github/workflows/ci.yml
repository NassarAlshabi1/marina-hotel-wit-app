Skip to content
marina-hotel-wit-app
Repository navigation
Code
Actions
CI
feat(mobile): enable auto-sync on network restore using existing Sync… #2693
Workflow file for this run
.github/workflows/ci.yml at 7286d0e
name: CI
permissions:
  contents: read
  pull-requests: write

on:
  push:
    branches:
      - main
      - 'capy/**'
      - 'feature/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true
      
      - name: Install dependencies
        run: flutter pub get
      
      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs
      
      # تحليل أساسي
      - name: Flutter Analyze
        run: flutter analyze --no-pub
      
      # Flutter Format (احتياطي - تحذير فقط)
      - name: Dart Format
        run: dart format --set-exit-if-changed .
        continue-on-error: true
      
      # اختبارات مع التغطية
      - name: Run tests
        run: flutter test --coverage
      
      - name: Coverage summary
        run: |
          sudo apt-get update -y
          sudo apt-get install -y lcov
          lcov --summary coverage/lcov.info | tee /tmp/coverage-summary.txt
          {
            echo "## Coverage summary";
            tail -n 4 /tmp/coverage-summary.txt;
          } >> "$GITHUB_STEP_SUMMARY"
      
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: mobile/coverage
