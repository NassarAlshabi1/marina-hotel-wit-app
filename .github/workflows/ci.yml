concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

name: CI
permissions:
  contents: read
  pull-requests: write

# ═══════════════════════════════════════════════════════════════
# يعمل على كل الفروع
# ═══════════════════════════════════════════════════════════════
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: mobile

    steps:
      - uses: actions/checkout@v5

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Flutter Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Dart Format
        run: dart format --set-exit-if-changed .
        continue-on-error: true

      # ═══════════════════════════════════════════════════════════
      #  اختبارات
      #  --exclude-tags integration: يستثني اختبارات tagged بـ @Tags(['integration'])
      #  --concurrency=2: تقليل ضغط الذاكرة
      #  --timeout=60s: timeout قصير لمنع التعليق
      #  test/flutter_test_config.dart يُهيِّئ sqfliteFfiInit() تلقائياً
      # ═══════════════════════════════════════════════════════════
      - name: Run tests
        run: flutter test --coverage --concurrency=2 --timeout=60s --exclude-tags integration --reporter compact
        continue-on-error: true

      - name: Coverage summary
        if: always()
        run: |
          sudo apt-get update -y -qq
          sudo apt-get install -y -qq lcov > /dev/null 2>&1
          if [ -f coverage/lcov.info ]; then
            lcov --summary coverage/lcov.info | tee /tmp/coverage-summary.txt
            # استخراج نسبة التغطية الإجمالية
            COVERAGE_PCT=$(grep "lines" /tmp/coverage-summary.txt | grep -oE '[0-9.]+%' | head -1 | tr -d '%')
            echo "COVERAGE_PCT=$COVERAGE_PCT" >> $GITHUB_ENV
            echo "Coverage: ${COVERAGE_PCT}%"
            {
              echo "## 📊 Coverage Summary"
              echo ""
              echo "**Branch:** \`${{ github.ref_name }}\`"
              echo "**Coverage:** ${COVERAGE_PCT}%"
              echo ""
              echo '```'
              tail -n 4 /tmp/coverage-summary.txt
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## 📊 Coverage" >> "$GITHUB_STEP_SUMMARY"
            echo "No coverage file generated." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: 🚦 Coverage gate (minimum 70%)
        if: always() && env.COVERAGE_PCT != ''
        run: |
          echo "Coverage: ${COVERAGE_PCT}%"
          if (( $(echo "${COVERAGE_PCT} < 70.0" | bc -l) )); then
            echo "❌ Coverage ${COVERAGE_PCT}% is below minimum 70%"
            exit 1
          else
            echo "✅ Coverage ${COVERAGE_PCT}% meets minimum 70%"
          fi

      - name: 💬 Comment PR with coverage
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            let coverage = '⚠️ No coverage data';
            try {
              const summary = fs.readFileSync('/tmp/coverage-summary.txt', 'utf8');
              const match = summary.match(/lines\.*:\s*([\d.]+)%/);
              if (match) {
                const pct = parseFloat(match[1]);
                const emoji = pct >= 70 ? '✅' : pct >= 50 ? '🟡' : '🔴';
                coverage = `${emoji} Coverage: ${pct}%`;
              }
            } catch (e) {
              console.log('No coverage file:', e.message);
            }

            const body = `## 📊 Test Coverage\n\n${coverage}\n\n_Updated automatically by CI._`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('Test Coverage')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4.6.2
        with:
          name: coverage-report-${{ github.ref_name }}-${{ github.run_id }}
          path: mobile/coverage
          if-no-files-found: ignore
          retention-days: 14
