{
  "name": "marina-hotel-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute marina-hotel-db --file=./schema.sql",
    "db:seed": "wrangler d1 execute marina-hotel-db --file=./seed.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.94.0"
  },
  "dependencies": {}
}
