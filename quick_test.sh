#!/bin/bash

# ============================================================================
# تشغيل سريع لاختبارات Supabase | Quick Supabase Test Runner
# ============================================================================
# الاستخدام | Usage: ./quick_test.sh
# ============================================================================

set -e

echo "🚀 Marina Hotel - تشغيل سريع لاختبارات Supabase"
echo "=================================================="
echo ""

# ============================================================================
# 1. التحقق من Flutter
# ============================================================================
if ! command -v flutter &> /dev/null; then
    echo "❌ Flutter غير مثبت"
    echo "   ثبت Flutter من: https://flutter.dev"
    exit 1
fi

echo "✅ Flutter مثبت: $(flutter --version | head -n 1)"
echo ""

# ============================================================================
# 2. تثبيت المكتبات
# ============================================================================
echo "📦 تثبيت المكتبات..."
cd mobile
flutter pub get > /dev/null 2>&1
cd ..
echo "✅ تم تثبيت المكتبات"
echo ""

# ============================================================================
# 3. تشغيل الاختبارات
# ============================================================================
echo "🧪 تشغيل اختبارات Supabase..."
echo "=================================================="
echo ""

flutter test test/supabase_sync_test.dart

# ============================================================================
# 4. النتيجة
# ============================================================================
if [ $? -eq 0 ]; then
    echo ""
    echo "=================================================="
    echo "✅ نجحت جميع الاختبارات! 🎉"
    echo "=================================================="
else
    echo ""
    echo "=================================================="
    echo "❌ بعض الاختبارات فشلت"
    echo "=================================================="
    echo ""
    echo "📋 الخطوات التالية:"
    echo "  1. راجع الأخطاء أعلاه"
    echo "  2. تأكد من إعداد قاعدة البيانات"
    echo "  3. تأكد من نشر Edge Functions"
    echo "  4. راجع SUPABASE_SETUP_GUIDE.md"
    exit 1
fi
