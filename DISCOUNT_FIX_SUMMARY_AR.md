# تقرير إصلاح مشكلة مزامنة التخفيضات 

## المشكلة
بعد إعادة تنصيب التطبيق وسحب (مزامنة) البيانات من Appwrite Cloud، لم يتم احتساب التخفيضات ("ليالي مخفضة") في إجمالي المبلغ، رغم ظهور إعدادات التخفيض في واجهة المستخدم (مثل خصم 2000 من 7000).

## السبب
يعتمد التطبيق على نظامين لتسعير الحجوزات:
1. **النظام القديم**: يخزن قيمة التخفيض مباشرة في سجل الحجز (`Bookings`).
2. **النظام الجديد**: يستخدم جدول منفصل للتعديلات المالية (`booking_price_adjustments`).

عند إنشاء حجز جديد محليًا، يقوم التطبيق تلقائيًا بتحويل "التخفيض القديم" إلى "سجل تعديل جديد". ولكن أثناء **المزامنة من السحاب (Sync)**، كانت هذه الخطوة مفقودة. التطبيق يقوم بتحميل بيانات الحجز ولكنه لا ينشئ سجل التعديل المالي المقابل، مما جعل محرك حساب الأسعار يتجاهل التخفيض تمامًا.

## الحل التقني
تم تحديث كود المزامنة لضمان معالجة التخفيضات فور تحميل البيانات:

1. **تعديل `BookingsRepository`**:
   - تم تحويل الدالة `_syncLegacyDiscountToAdjustments` إلى دالة عامة `syncLegacyDiscountToAdjustments` لتصبح قابلة للاستدعاء من خارج المستودع.

2. **تحديث `AppwriteSyncManager`**:
   - تمت إضافة خطوة جديدة داخل عملية `_syncBookings`.
   - فور انتهاء مزامنة أي حجز، يقوم النظام الآن بـ:
     1. استدعاء الدالة `syncLegacyDiscountToAdjustments` للتحقق من وجود تخفيض وتحويله إلى النظام الجديد.
     2. استدعاء `refreshForBookingId` لإعادة حساب كافة التفاصيل المالية (عدد الليالي، الإجمالي، والمتبقي) بناءً على التعديلات الجديدة.

## النتيجة
الآن عند سحب البيانات (المزامنة):
1. يتم حفظ الحجز في قاعدة البيانات المحلية.
2. يكتشف النظام وجود قيمة تخفيض.
3. يقوم بإنشاء سجل في جدول التعديلات المالية (`adjustments`).
4. يتم إعادة حساب سعر الليلة (مثلاً: 7,000 - 2,000 = 5,000) وتحديث الإجمالي الظاهر في الواجهة تلقائيًا.
