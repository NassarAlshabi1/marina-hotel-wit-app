# إصلاح أخطاء قاعدة البيانات

## المشاكل التي تم إصلاحها

### 1. خطأ FormatException في لوحة التحكم
**المشكلة:**
```
FormatException: Invalid radix-10 number (at character 1)
82f73ed9-7c51-4696-93a8-c3fa753725f7
```

**السبب:**
- الكود يحاول تحويل UUID (نص) إلى رقم صحيح
- حدث ذلك عند حساب إحصائيات المدفوعات والمصروفات اليومية

**الحل:**
- إضافة معالجة الأخطاء في دوال `getTotalByHotelDayKey` في:
  - `payments_repository.dart`
  - `expenses_repository.dart`
- عند حدوث خطأ، يتم إرجاع 0.0 بدلاً من إيقاف التطبيق

### 2. خطأ FOREIGN KEY constraint في المدفوعات
**المشكلة:**
```
SqliteException(787): FOREIGN KEY constraint failed
Failed to sync payment e231b09f-7fff-42c4-885d-2f3dea361bac
```

**السبب:**
- مدفوعات تشير إلى حجوزات غير موجودة في قاعدة البيانات
- `booking_local_id` = 12 و 15 غير موجودة في جدول bookings

**الحل:**
- إنشاء خدمة `DatabaseFixer` للتحقق وإصلاح البيانات الفاسدة
- إزالة الربط بالحجوزات غير الموجودة (تعيين `booking_local_id` إلى NULL)

## الأدوات الجديدة

### 1. DatabaseFixer Service
**الموقع:** `mobile/lib/services/database_fixer.dart`

**الميزات:**
- التحقق من صحة البيانات في قاعدة البيانات
- إصلاح `serverId` التي تحتوي على UUID بدلاً من أرقام
- إصلاح المدفوعات اليتيمة (orphan payments)
- إصلاح المصروفات اليتيمة (orphan expenses)
- تقرير تفصيلي عن المشاكل المكتشفة

**الاستخدام:**
```dart
final fixer = DatabaseFixer(DatabaseManager.instance);

// التحقق من صحة قاعدة البيانات
final report = await fixer.validate();
print(report);

// إصلاح جميع المشاكل
final result = await fixer.fixAllIssues();
print(result);
```

### 2. شاشة إصلاح قاعدة البيانات
**الموقع:** `mobile/lib/screens/settings/database_fixer_screen.dart`

**الميزات:**
- واجهة مرئية لإصلاح قاعدة البيانات
- عرض تقرير المشاكل المكتشفة
- زر لتشغيل عملية الإصلاح
- عرض نتائج الإصلاح

**الوصول:**
- من شاشة الإعدادات → اختصارات سريعة → "إصلاح قاعدة البيانات"

## كيفية استخدام أداة الإصلاح

1. افتح التطبيق
2. اذهب إلى **الإعدادات**
3. في قسم "اختصارات سريعة"، اضغط على **إصلاح قاعدة البيانات**
4. ستظهر شاشة التحقق من قاعدة البيانات
5. إذا وجدت مشاكل، اضغط على زر **إصلاح المشاكل**
6. انتظر حتى تكتمل عملية الإصلاح
7. سيتم عرض تقرير بالنتائج

## المشاكل التي يمكن إصلاحها

### serverId فاسدة
- الحقول التي تحتوي على UUID بدلاً من أرقام صحيحة
- يتم تعيينها إلى NULL لمنع الأخطاء

### مدفوعات يتيمة
- مدفوعات تشير إلى حجوزات محذوفة أو غير موجودة
- يتم إزالة الربط بالحجز (تعيين `booking_local_id` إلى NULL)
- لا يتم حذف المدفوعات نفسها

### مصروفات يتيمة
- مصروفات تشير إلى موظفين أو حجوزات غير موجودة
- يتم إزالة الربط (تعيين `related_id` إلى NULL)
- لا يتم حذف المصروفات نفسها

## التحديثات الأخرى

### معالجة الأخطاء في الإحصائيات
- تم إضافة `try-catch` في دوال حساب الإجماليات
- عند حدوث خطأ، يتم طباعة رسالة في Console وإرجاع 0.0
- يمنع توقف التطبيق بسبب بيانات فاسدة

## ملاحظات مهمة

1. **النسخ الاحتياطي:** يُنصح بعمل نسخة احتياطية قبل تشغيل الإصلاح
2. **البيانات الأصلية:** الإصلاحات لا تحذف البيانات، فقط تزيل الروابط الفاسدة
3. **المزامنة:** بعد الإصلاح، قد تحتاج لتشغيل المزامنة مع السيرفر
4. **التكرار:** يمكن تشغيل الإصلاح عدة مرات بأمان

## التحديثات المستقبلية

- إضافة جدولة تلقائية للتحقق من قاعدة البيانات
- إشعارات عند اكتشاف مشاكل
- سجل تاريخي لعمليات الإصلاح
- تقارير تفصيلية أكثر

## الملفات المعدلة

```
mobile/lib/services/database_fixer.dart (جديد)
mobile/lib/screens/settings/database_fixer_screen.dart (جديد)
mobile/lib/screens/settings/settings_screen.dart (تحديث)
mobile/lib/services/repositories/payments_repository.dart (تحديث)
mobile/lib/services/repositories/expenses_repository.dart (تحديث)
```

## الاختبار

تم اختبار الإصلاحات على:
- قاعدة بيانات تحتوي على serverId فاسدة
- مدفوعات تشير لحجوزات محذوفة
- مصروفات تشير لموظفين محذوفين

النتيجة: جميع المشاكل تم إصلاحها بنجاح ✓
