# دليل نظام المزامنة التلقائية الذكي
## Marina Hotel Smart Sync System

تحديث: 28 أكتوبر 2025

---

## نظرة عامة

نظام المزامنة التلقائية الذكي في تطبيق Marina Hotel يحل مشكلة **مزامنة البيانات تلقائياً بين أجهزة متعددة** تستخدم نفس التطبيق. النظام يعمل بذكاء لاكتشاف التحديثات الجديدة من الأجهزة الأخرى ومزامنتها تلقائياً مع حل التضارب بطرق متقدمة.

### الميزات الرئيسية:
- ✅ **مراقبة تلقائية**: فحص دوري لـ Google Drive (كل 1-60 دقيقة قابل للتخصيص)
- 🆔 **تمييز الأجهزة**: كل جهاز له device_id فريد
- 🤝 **حل التضارب الذكي**: 3 استراتيجيات متقدمة للتعامل مع التضارب
- 📱 **إشعارات تفاعلية**: تنبيهات عند المزامنة والأخطاء
- ⚡ **تحسين الأداء**: نظام ذكي لتوفير البطارية والبيانات
- 🔄 **مزامنة يدوية**: إمكانية المزامنة الفورية

---

## كيفية العمل

```
📱 جهاز A: إضافة حجز جديد
    ↓ (30 ثانية)
☁️ رفع نسخة احتياطية لـ Google Drive مع device_id
    ↓ (5 دقائق - فترة الفحص)
🔍 جهاز B: فحص Google Drive للنسخ الجديدة
    ↓ 
🆕 اكتشاف نسخة جديدة من device_id مختلف
    ↓
📥 تحميل ومقارنة البيانات
    ↓
🤝 حل أي تضارب (الأحدث يفوز افتراضياً)
    ↓
✅ مزامنة البيانات تلقائياً
    ↓
🎉 إشعار المستخدم بنجاح المزامنة
```

---

## إعداد النظام

### المتطلبات الأساسية:
1. **تسجيل دخول Google Drive**: يجب أن يكون المستخدم مسجلاً في Google Drive
2. **اتصال بالإنترنت**: للوصول لـ Google Drive API
3. **تفعيل النسخ الاحتياطي التلقائي**: يجب أن يكون مفعلاً

### خطوات التفعيل:

#### 1. الوصول لإعدادات المزامنة:
```
الإعدادات → المزامنة التلقائية الذكية
```

#### 2. تفعيل النظام:
- تأكد من تسجيل الدخول في Google Drive
- فعّل "المزامنة التلقائية بين الأجهزة"
- اختر فترة الفحص (الافتراضي: 5 دقائق)

#### 3. اختيار استراتيجية حل التضارب:
- **الأحدث يفوز** (موصى به): البيانات الأحدث تاريخياً تحل محل الأقدم
- **حل يدوي**: إيقاف المزامنة وطلب تدخل المستخدم
- **أولوية الجهاز**: للهيكل الإداري المتقدم

---

## استراتيجيات حل التضارب

### 1. الأحدث يفوز (Newer Wins) - الافتراضي
- **المبدأ**: البيانات التي تم تعديلها مؤخراً تحل محل الأقدم
- **الاستخدام**: مناسب لمعظم الحالات
- **المزايا**: آمن وتلقائي بالكامل
- **مثال**: إذا تم تعديل حجز على الجهاز A الساعة 2:00 وعلى الجهاز B الساعة 2:05، ستُعتمد نسخة الجهاز B

### 2. الحل اليدوي (Manual Resolve)
- **المبدأ**: إيقاف المزامنة وإشعار المستخدم لحل التضارب
- **الاستخدام**: للبيانات الحساسة التي تحتاج مراجعة
- **المزايا**: تحكم كامل من المستخدم
- **العيوب**: يتطلب تدخل يدوي

### 3. أولوية الجهاز (Device Priority)
- **المبدأ**: جهاز معين له الأولوية في كل التضارب
- **الاستخدام**: للهيكل الإداري (جهاز المدير له أولوية)
- **المتطلبات**: تحديد الجهاز الرئيسي مسبقاً

---

## الواجهات والمكونات

### 1. شاشة الإعدادات (`SmartSyncSettingsScreen`)
**المسار**: `الإعدادات → المزامنة التلقائية الذكية`

#### الأقسام:
- **بطاقة الحالة**: عرض حالة النظام في الوقت الفعلي
- **إعدادات التفعيل**: تشغيل/إيقاف النظام
- **فترة الفحص**: اختيار كم مرة يفحص النظام (1-60 دقيقة)
- **حل التضارب**: اختيار الاستراتيجية
- **الإجراءات**: مزامنة يدوية وتحديث الحالة

### 2. بطاقة المزامنة في لوحة التحكم (`SmartSyncDashboardCard`)
**الموقع**: الشاشة الرئيسية (Dashboard)

#### المعلومات المعروضة:
- حالة النظام (مفعل/معطل)
- فترة الفحص الحالية
- حالة المزامنة (جاري/متوقف)
- آخر فحص تم

### 3. مؤشر الحالة (`SmartSyncStatusWidget`)
**الموقع**: شريط التطبيق (AppBar)

#### الحالات:
- 🟢 **مُزامن**: النظام يعمل وآخر فحص كان ناجح
- 🔵 **مزامنة...**: جاري تحميل أو مزامنة البيانات
- ⚫ **مخفي**: النظام معطل أو غير متاح

### 4. زر المزامنة السريعة (`SmartSyncFloatingButton`)
**الموقع**: زر عائم في الشاشات الرئيسية

#### الاستخدام:
- اضغط للمزامنة الفورية
- يظهر فقط عندما يكون النظام مفعلاً
- يتحول لـ loading أثناء المزامنة

---

## إدارة الإشعارات

### أنواع الإشعارات:

#### 1. إشعار نجاح المزامنة
- **المحتوى**: عدد السجلات المُزامنة ومصدرها
- **المدة**: 5 ثوان
- **اللون**: أخضر
- **الإجراءات**: إغلاق

#### 2. إشعار فشل المزامنة
- **المحتوى**: سبب الخطأ
- **المدة**: 7 ثوان
- **اللون**: أحمر
- **الإجراءات**: إعادة المحاولة، إغلاق

#### 3. إشعار اكتشاف بيانات جديدة
- **المحتوى**: عدد التغييرات والجهاز المصدر
- **المدة**: 10 ثوان
- **اللون**: أزرق
- **الإجراءات**: مزامنة تلقائية، عرض التفاصيل

#### 4. إشعار بدء المزامنة
- **المحتوى**: "بدأت المزامنة التلقائية..."
- **المدة**: 2 ثانية
- **اللون**: أزرق

---

## تحسين الأداء

### ميزات توفير الطاقة:
- **فترة ديناميكية**: تطويل فترة الفحص عند عدم وجود تغييرات
- **توقف ذكي**: إيقاف المراقبة عند انخفاض البطارية
- **فحص البيانات**: احترام حدود استهلاك البيانات

### إعدادات موصى بها:

#### للاستخدام المكثف:
- **فترة الفحص**: 1-2 دقيقة
- **حل التضارب**: الأحدث يفوز
- **مناسب لـ**: المكاتب بعدة موظفين

#### للاستخدام العادي:
- **فترة الفحص**: 5-10 دقائق
- **حل التضارب**: الأحدث يفوز
- **مناسب لـ**: الفنادق الصغيرة

#### لتوفير البطارية:
- **فترة الفحص**: 15-30 دقيقة
- **حل التضارب**: الأحدث يفوز
- **مناسب لـ**: الأجهزة القديمة أو الاستخدام المتقطع

---

## استكشاف الأخطاء

### المشاكل الشائعة وحلولها:

#### 1. "النظام غير مفعل رغم تشغيله"
**السبب**: عدم تسجيل الدخول في Google Drive
**الحل**: 
```
الإعدادات → Google Drive → تسجيل الدخول
```

#### 2. "لا تتم المزامنة التلقائية"
**الأسباب المحتملة**:
- انقطاع الإنترنت
- انتهاء صلاحية Google Drive token
- امتلاء مساحة Google Drive

**الحلول**:
```
1. التحقق من الاتصال بالإنترنت
2. إعادة تسجيل الدخول في Google Drive
3. تنظيف مساحة Google Drive
4. مزامنة يدوية لاختبار الاتصال
```

#### 3. "تضارب البيانات مستمر"
**السبب**: أجهزة متعددة تعدل نفس البيانات بنفس الوقت
**الحل**: 
```
1. تنسيق العمل بين المستخدمين
2. تغيير استراتيجية حل التضارب لـ "أولوية الجهاز"
3. استخدام "الحل اليدوي" للبيانات الحساسة
```

#### 4. "استنزاف البطارية"
**الحل**:
```
1. زيادة فترة الفحص (15-30 دقيقة)
2. تفعيل "التوقف الذكي" في الإعدادات
3. إيقاف المزامنة عند عدم الحاجة
```

#### 5. "استنزاف البيانات"
**الحل**:
```
1. تحديد حد البيانات اليومي
2. استخدام WiFi بدلاً من البيانات المحمولة
3. تقليل فترة الفحص
```

---

## المراقبة والصيانة

### معلومات الحالة المتاحة:
- **معرف الجهاز**: للتعرف على الجهاز الحالي
- **آخر فحص**: موقت آخر مرة تحقق النظام من Google Drive
- **حالة الاتصال**: Google Drive متاح/غير متاح
- **إحصائيات المزامنة**: نجح/فشل، عدد السجلات

### سجلات النظام:
```dart
// في Flutter Debug Console:
🔄 مدير المزامنة الذكي: تم التهيئة بنجاح
🆔 معرف الجهاز: marina_SM-G991B_Galaxy S21_1635422400000
⏰ بدء مراقبة المزامنة المُحسَّنة كل 5 دقائق
🔍 فحص وجود نسخ احتياطية جديدة...
🆕 تم العثور على نسخة احتياطية جديدة من جهاز آخر
🔄 بدء مزامنة النسخة الجديدة...
✅ تمت المزامنة بنجاح
🎉 تمت مزامنة البيانات من marina_iPhone14_Pro_1635422500000
```

---

## API والتطوير

### استخدام النظام في الكود:

#### الحصول على مدير المزامنة:
```dart
final manager = SmartSyncManager.instance;
```

#### التحقق من الحالة:
```dart
final status = await manager.getStatus();
print('مفعل: ${status['enabled']}');
print('جاري المزامنة: ${status['is_syncing']}');
```

#### تغيير الإعدادات:
```dart
// تفعيل/إيقاف
await manager.setEnabled(true);

// تغيير فترة الفحص
await manager.setSyncInterval(10); // 10 دقائق

// تغيير استراتيجية التضارب
await manager.setConflictResolution(ConflictResolution.newerWins);
```

#### مزامنة يدوية:
```dart
try {
  await manager.forceSyncNow();
  print('تمت المزامنة بنجاح');
} catch (e) {
  print('خطأ في المزامنة: $e');
}
```

#### مراقبة التغييرات:
```dart
// في Widget
final statusAsync = ref.watch(smartSyncStatusProvider);

statusAsync.when(
  loading: () => CircularProgressIndicator(),
  error: (error, stack) => Text('خطأ: $error'),
  data: (status) => Text('الحالة: ${status['enabled']}'),
);
```

---

## الأمان والخصوصية

### حماية البيانات:
- **تشفير**: جميع البيانات مشفرة قبل الرفع لـ Google Drive
- **المصادقة**: استخدام OAuth 2.0 آمن مع Google
- **الصلاحيات**: الوصول محدود لمجلد التطبيق فقط

### معرف الجهاز:
- **التكوين**: `marina_{device_name}_{device_model}_{timestamp}`
- **الغرض**: التمييز بين الأجهزة المختلفة
- **الخصوصية**: لا يحتوي على معلومات شخصية

### البيانات المخزنة محلياً:
- إعدادات المزامنة
- آخر timestamp للمزامنة
- معرف الجهاز
- إحصائيات الأداء

---

## دعم متعدد الأجهزة

### أنواع الأجهزة المدعومة:
- ✅ **Android**: جميع الإصدارات المدعومة بالتطبيق
- ✅ **iOS**: iPhone وiPad
- ⚠️ **ويب**: محدود (يحتاج Google Drive API للويب)

### اعتبارات خاصة:
- **التوقيت**: يجب أن تكون أوقات الأجهزة متزامنة
- **المساحة**: تأكد من توفر مساحة كافية على Google Drive
- **الشبكة**: اتصال مستقر بالإنترنت لجميع الأجهزة

---

## الخلاصة والتوصيات

### أفضل الممارسات:

1. **فعّل النظام على جميع الأجهزة** لضمان المزامنة الكاملة
2. **استخدم "الأحدث يفوز"** كاستراتيجية افتراضية
3. **راقب الإشعارات** للتأكد من نجاح المزامنة
4. **أعد تشغيل التطبيق** بعد تغيير إعدادات مهمة
5. **احتفظ بنسخ احتياطية منتظمة** كإجراء إضافي

### في حالة الطوارئ:
- **إيقاف المزامنة فوراً** إذا كانت تسبب مشاكل
- **استخدام المزامنة اليدوية** للتحكم الكامل
- **الرجوع للنسخ الاحتياطية العادية** إذا فشل النظام الذكي

---

**آخر تحديث**: 28 أكتوبر 2025  
**الإصدار**: 1.0  
**المطور**: Capy AI  
**الدعم التقني**: متاح عبر GitHub Issues  

---

*هذا الدليل جزء من مشروع تحسين تطبيق Marina Hotel. جميع الميزات تم تطويرها وفقاً لأحدث معايير الأمان والأداء.*