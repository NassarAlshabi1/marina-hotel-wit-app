import 'package:flutter_test/flutter_test.dart';
import 'package:marina_hotel_mobile/services/unified_sync_orchestrator.dart';

void main() {
  group('UnifiedSyncOrchestrator', () {
    test('should exist as a class', () {
      expect(UnifiedSyncOrchestrator, isNotNull);
    });
  });
}
