## PR Code Suggestions ✨

<!-- b215415 -->

Explore these optional code suggestions:

<table><thead><tr><td><strong>Category</strong></td><td align=left><strong>Suggestion&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; </strong></td><td align=center><strong>Impact</strong></td></tr><tbody><tr><td rowspan=1>High-level</td>
<td>



<details><summary>Re-evaluate the synchronization architecture</summary>

___

**The current implementation introduces multiple redundant synchronization systems <br>for Appwrite, Google Drive, and a generic API, which can cause conflicts. It is <br>recommended to refactor this into a unified architecture with a single sync <br>engine and swappable backend adapters for improved robustness and <br>maintainability.**


### Examples:



<details>
<summary>
<a href="https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-c3ce10d1f25a663f4ee7caea430046eb71658a18b63e424392dbff4c67e0ad1cR59-R1614">sync/services/appwrite_sync_manager.dart [59-1614]</a>
</summary></details>



<details>
<summary>
<a href="https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-616099eece02f842e140067261394d40037c8aa62ed655803c14a98f32278929R25-R720">sync/services/google_drive_delta_sync.dart [25-720]</a>
</summary></details>




### Solution Walkthrough:



#### Before:
```dart
// Multiple, independent sync services exist and can be called in parallel,
// leading to potential race conditions and data integrity issues.

// System 1: Appwrite-specific sync
class AppwriteSyncManager {
  Future<SyncResult> sync() { ... }
}

// System 2: Google Drive delta sync
class GoogleDriveDeltaSync {
  Future<DeltaSyncResult> pushDeltaChanges() { ... }
  Future<DeltaSyncResult> pullDeltaChanges() { ... }
}

// System 3: Generic API-based sync
class SyncService {
  Future<void> runSync() {
    _push();
    _pull();
  }
}

```



#### After:
```dart
// A single, unified sync engine with a clear strategy.
// Different backends are handled by swappable adapters,
// preventing conflicting parallel operations.

abstract class SyncAdapter {
  Future<void> push(List<Change> changes);
  Future<List<Change>> pull(int lastSyncTimestamp);
}

class AppwriteAdapter implements SyncAdapter { ... }
class GoogleDriveDeltaAdapter implements SyncAdapter { ... }

class UnifiedSyncEngine {
  final SyncAdapter adapter;
  UnifiedSyncEngine(this.adapter);

  Future<void> sync() {
    // Coordinated push and pull logic using the selected adapter
  }
}

```




<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: The suggestion correctly identifies a critical architectural flaw where multiple, parallel synchronization systems (`AppwriteSyncManager`, `GoogleDriveDeltaSync`, `SyncService`) are introduced, creating redundancy and a high risk of data conflicts and maintenance issues.


</details></details></td><td align=center>High

</td></tr><tr><td rowspan=7>Possible issue</td>
<td>



<details><summary>Prevent deadlocks by ensuring mutex release</summary>

___

**Wrap the core logic of the <code>sync</code> method in a <code>try...finally</code> block to ensure <br><code>_mutex.release()</code> is always called, preventing potential deadlocks.**

[sync/services/appwrite_sync_manager.dart [332-545]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-c3ce10d1f25a663f4ee7caea430046eb71658a18b63e424392dbff4c67e0ad1cR332-R545)

```diff
     Future<SyncResult> sync({bool push = true, bool pull = true}) async {
       if (!await _mutex.acquire()) {
         _logger.warning('Failed to acquire sync mutex', tag: 'SYNC');
         return SyncResult(
           status: SyncStatus.failed,
           errorMessage: 'Sync mutex timeout',
           timestamp: DateTime.now(),
           duration: Duration.zero,
         );
       }
       
-      if (_currentStatus == SyncStatus.syncing) {
-        _logger.warning('Sync already in progress', tag: 'SYNC');
+      try {
+        if (_currentStatus == SyncStatus.syncing) {
+          _logger.warning('Sync already in progress', tag: 'SYNC');
+          return SyncResult(
+            status: SyncStatus.failed,
+            errorMessage: 'Sync already in progress',
+            timestamp: DateTime.now(),
+            duration: Duration.zero,
+          );
+        }
+
+        _currentStatus = SyncStatus.syncing;
+        _syncController.add(_currentStatus);
+        
+        final startTime = DateTime.now();
+
+        final metrics = SyncMetrics.instance;
+        metrics.startSync();
+
+        final phaseMs = <String, int>{};
+        int recordsPushed = 0;
+        int recordsPulled = 0;
+        int conflicts = 0;
+        String? errorMessage;
+        SyncStatus finalStatus = SyncStatus.success;
+        late String syncLogId;
+        late String syncLogLocalUuid;
+        int syncLogVersion = 1;
+        bool hasSyncLog = false;
+        int? syncLogCreatedEpoch;
+
+        try {
+          _logger.info('Starting sync...', tag: 'SYNC');
+
+          // التحقق من الاتصال
+          final connectivityResult = await Connectivity().checkConnectivity();
+          final hasConnection = connectivityResult is List<ConnectivityResult>
+              ? connectivityResult.any((r) => r != ConnectivityResult.none)
+              : connectivityResult != ConnectivityResult.none;
+          if (!hasConnection) {
+            throw Exception('No internet connection');
+          }
+...
+        } catch (e, stackTrace) {
+...
+          _logger.error('Sync failed', error: e, stackTrace: stackTrace, tag: 'SYNC');
+        }
+
+        _currentStatus = finalStatus;
+        _syncController.add(_currentStatus);
+
+        final endTime = DateTime.now();
+        final duration = endTime.difference(startTime);
+...
+      } finally {
         _mutex.release();
-        return SyncResult(
-          status: SyncStatus.failed,
-          errorMessage: 'Sync already in progress',
-          timestamp: DateTime.now(),
-          duration: Duration.zero,
-        );
       }
-
-      _currentStatus = SyncStatus.syncing;
-      _syncController.add(_currentStatus);
-      
-      final startTime = DateTime.now();
-
-      final metrics = SyncMetrics.instance;
-      metrics.startSync();
-
-      final phaseMs = <String, int>{};
-      int recordsPushed = 0;
-      int recordsPulled = 0;
-      int conflicts = 0;
-      String? errorMessage;
-      SyncStatus finalStatus = SyncStatus.success;
-      late String syncLogId;
-      late String syncLogLocalUuid;
-      int syncLogVersion = 1;
-      bool hasSyncLog = false;
-      int? syncLogCreatedEpoch;
-
-      try {
-        _logger.info('Starting sync...', tag: 'SYNC');
-
-        // التحقق من الاتصال
-        final connectivityResult = await Connectivity().checkConnectivity();
-        final hasConnection = connectivityResult is List<ConnectivityResult>
-            ? connectivityResult.any((r) => r != ConnectivityResult.none)
-            : connectivityResult != ConnectivityResult.none;
-        if (!hasConnection) {
-          throw Exception('No internet connection');
-        }
-...
-      } catch (e, stackTrace) {
-...
-        _logger.error('Sync failed', error: e, stackTrace: stackTrace, tag: 'SYNC');
-      }
-
-      _currentStatus = finalStatus;
-      _syncController.add(_currentStatus);
-      _mutex.release();
-
-      final endTime = DateTime.now();
-      final duration = endTime.difference(startTime);
 ...
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: This suggestion correctly identifies a critical concurrency issue where a mutex might not be released, leading to a deadlock. The proposed `try...finally` block is the correct and robust solution to ensure the resource is always freed.


</details></details></td><td align=center>High

</td></tr><tr><td>



<details><summary>Ensure database integrity after restore</summary>

___

**In <code>restoreFromBackup</code>, move the <code>PRAGMA foreign_keys = ON</code> statement into the <br><code>finally</code> block to ensure foreign key constraints are always re-enabled, even if <br>the restore process fails.**

[sync/services/google_drive_backup_service.dart [679-980]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-e3b4109c65d5ed43fec23a4a970f9cd4d76dc29098e91519e42a9ef9b05b57e0R679-R980)

```diff
     Future<void> restoreFromBackup(Map<String, dynamic> backupData) async {
       try {
         final db = DatabaseManager.instance;
 
         if (!backupData.containsKey('metadata')) {
           throw Exception('النسخة الاحتياطية لا تحتوي على بيانات وصفية');
         }
 
         final metadataJson = backupData['metadata'];
         if (metadataJson is! Map) {
           throw Exception('صيغة بيانات النسخة الاحتياطية غير صالحة');
         }
         final metadata = BackupMetadata.fromJson(Map<String, dynamic>.from(metadataJson));
         _logger.info('بدء استعادة نسخة بتاريخ ${metadata.backupTimestamp.toIso8601String()} تحتوي ${metadata.totalRecords} سجل', tag: 'RESTORE');
 
         if (metadata.databaseVersion > 3) {
           throw Exception('إصدار قاعدة البيانات في النسخة الاحتياطية أحدث من التطبيق الحالي');
         }
 
         _log('🔄 بدء استعادة البيانات...');
 
         // تعطيل FOREIGN KEYS أثناء الحذف والاستعادة بالكامل
         await db.customStatement('PRAGMA foreign_keys = OFF');
         try {
           await db.transaction(() async {
             // حذف جميع الجداول
             await db.delete(db.rooms).go();
             await db.delete(db.bookings).go();
 ...
             await db.delete(db.syncState).go();
 
-        // استعادة البيانات بالترتيب الصحيح (الجداول الرئيسية أولاً)
-        if (backupData.containsKey('rooms')) {
+            // استعادة البيانات بالترتيب الصحيح (الجداول الرئيسية أولاً)
+            if (backupData.containsKey('rooms')) {
 ...
-        }
-        });
+            }
+          });
 
-        // استعادة وتطبيق الإعدادات العامة إذا وجدت
-        if (backupData.containsKey('system_settings')) {
+          // استعادة وتطبيق الإعدادات العامة إذا وجدت
+          if (backupData.containsKey('system_settings')) {
 ...
-        }
+          }
 
-        _log('✅ تم استعادة ${metadata.totalRecords} سجل بنجاح');
-        final fixService = RestoreFixService(db);
-        await fixService.runAutoFixAfterRestore(backupTimestamp: metadata.backupTimestamp);
+          _log('✅ تم استعادة ${metadata.totalRecords} سجل بنجاح');
+          final fixService = RestoreFixService(db);
+          await fixService.runAutoFixAfterRestore(backupTimestamp: metadata.backupTimestamp);
         } finally {
           // إعادة تشغيل FOREIGN KEYS بعد الانتهاء من الاستعادة بالكامل
           await db.customStatement('PRAGMA foreign_keys = ON');
           _log('🔓 تم إعادة تشغيل FOREIGN KEYS');
         }
       } catch (e) {
         _log('❌ خطأ في استعادة البيانات: $e');
         rethrow;
       }
     }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: This suggestion correctly identifies a critical flaw where database foreign key constraints could be left permanently disabled on error, compromising data integrity. The proposed fix of moving the re-enabling command to the `finally` block is the correct solution.


</details></details></td><td align=center>High

</td></tr><tr><td>



<details><summary>Propagate critical database restore errors</summary>

___

**In <code>runAutoFixAfterRestore</code>, propagate the error from a failed snapshot <br>restoration to the <code>RestoreFixReport</code> instead of just printing it to the console.**

[sync/services/restore_fix_service.dart [248-255]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-f56939d65665eb4b5391d1091411db16fbcbc9b6510fad80e90101172a763cf4R248-R255)

```diff
+String? errorMessage = e.toString();
 // استعادة اللقطة الاحتياطية في حالة الفشل
 if (snapshot != null) {
   try {
     await _restoreFromSnapshot(snapshot.filePath);
     debugPrint('✅ تم استعادة البيانات من اللقطة الاحتياطية');
   } catch (restoreError) {
     debugPrint('❌ فشل في استعادة اللقطة الاحتياطية: $restoreError');
+    // Propagate the critical restore error
+    errorMessage = 'فشل الإصلاح التلقائي، وفشلت استعادة اللقطة الاحتياطية: $restoreError';
   }
 }
 
+final duration = DateTime.now().difference(startTime).inMilliseconds;
+
+return RestoreFixReport(
+  success: false,
+  bookingsFixed: 0,
+  roomsUpdated: 0,
+  paymentsRecalculated: 0,
+  changes: [],
+  error: errorMessage,
+  executedAt: startTime,
+  durationMs: duration,
+);
+
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: The suggestion correctly identifies a critical flaw where a catastrophic database restore failure is silenced, which could leave the database in a corrupt state without proper notification.


</details></details></td><td align=center>High

</td></tr><tr><td>



<details><summary>Pass full object to avoid race-conditions</summary>

___

**To prevent a potential race condition, pass the entire backup object to the <br><code>_showRestoreConfirmation</code> function instead of just its identifier, thus avoiding <br>a lookup that might fail.**

[sync/screens/comprehensive_backup_screen.dart [1098-1174]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-0127e1a8255c172204cf53ef27653e6665e42131863bce402e86e6fead2ab616R1098-R1174)

```diff
-void _showRestoreConfirmation(String identifier, BackupType type) {
+void _showRestoreConfirmation(dynamic backup, BackupType type) {
   final dateFormatter = DateFormat('yyyy/MM/dd - HH:mm', 'ar');
   
   String title, description, location;
   DateTime createdTime;
   int? recordsCount;
-  dynamic backup;
 
   if (type == BackupType.googleDrive) {
-    try {
-      backup = ref.read(availableBackupsProvider).firstWhere((b) => b.fileId == identifier);
-    } on StateError {
-      return;
-    }
     title = 'استعادة من Google Drive';
     description = 'سيتم تنزيل النسخة من Google Drive واستعادة البيانات';
     location = 'Google Drive';
     createdTime = backup.createdTime;
     recordsCount = int.tryParse(backup.metadata?['records_count']?.toString() ?? '0');
   } else {
-    try {
-      backup = ref.read(localBackupsProvider).firstWhere((b) => b.filePath == identifier);
-    } on StateError {
-      return;
-    }
     title = 'استعادة من النسخة المحلية';
     description = 'سيتم استعادة البيانات من النسخة المحفوظة محلياً';
     location = 'التخزين المحلي';
     createdTime = backup.createdTime;
     recordsCount = backup.metadata?.totalRecords;
   }
   
   showDialog(
     context: context,
     builder: (context) => AlertDialog(
 ...
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 8</summary>

__

Why: The suggestion correctly identifies a potential race condition where the backup list could change, causing the lookup to fail and silently return. Passing the full object is a robust solution that prevents this bug and improves reliability.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Fix incorrect booking ID resolution</summary>

___

**In <code>_applyPaymentChange</code>, fix the booking ID resolution logic to prevent <br>overwriting a correctly found <code>resolvedBookingLocalId</code>. The lookup by <br><code>serverBookingId</code> should only occur if the lookup by <code>incomingBookingUuid</code> fails.**

[sync/services/google_drive_delta_sync.dart [392-417]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-616099eece02f842e140067261394d40037c8aa62ed655803c14a98f32278929R392-R417)

```diff
 Future<void> _applyPaymentChange(AppDatabase db, String localUuid, String operation, Map<String, dynamic> data) async {
   if (operation == 'delete') {
     await (db.delete(db.payments)..where((t) => t.localUuid.equals(localUuid))).go();
     return;
   }
 
   final serverBookingId = _asInt(data['server_booking_id']) ?? _asInt(data['serverBookingId']);
   final incomingBookingUuid = _asString(data['booking_uuid_cache']) ??
       _asString(data['bookingUuidCache']) ??
       _asString(data['booking_uuid']) ??
       _asString(data['bookingUuid']);
 
   String? bookingUuidCache;
   int? resolvedBookingLocalId;
 
   if (incomingBookingUuid != null && incomingBookingUuid.isNotEmpty) {
     bookingUuidCache = incomingBookingUuid;
     final booking = await (db.select(db.bookings)..where((b) => b.localUuid.equals(incomingBookingUuid))).getSingleOrNull();
     resolvedBookingLocalId = booking?.id;
   }
 
   if (resolvedBookingLocalId == null && serverBookingId != null) {
     final booking = await (db.select(db.bookings)..where((b) => b.serverBookingId.equals(serverBookingId))).getSingleOrNull();
-    resolvedBookingLocalId = booking?.id;
-    bookingUuidCache = bookingUuidCache ?? booking?.localUuid;
+    if (booking != null) {
+      resolvedBookingLocalId = booking.id;
+      bookingUuidCache = bookingUuidCache ?? booking.localUuid;
+    }
   }
   ...
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 8</summary>

__

Why: The suggestion correctly identifies a logical flaw in booking ID resolution that could lead to data corruption by associating payments with incorrect bookings.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Prevent sync halt on single-item failure</summary>

___

**In the <code>_pull</code> method, avoid re-throwing errors within the transaction loop to <br>prevent a single failed item from rolling back the entire batch of changes. <br>Instead, log the error and continue processing.**

[sync/services/sync_service.dart [168-217]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-75d88958e817f8bd130124b2d5ff2bafaf53904f49b61d500bfe0450e512a034R168-R217)

```diff
 Future<void> _pull() async {
   final pullSettings = _performanceOptimizer.getCurrentPerformanceSettings();
   final pullTimeoutSeconds = (() {
     final custom = pullSettings['pullTimeout'];
     if (custom is int && custom > 0) {
       return custom;
     }
     final fallback = pullSettings['timeout'];
     if (fallback is int && fallback > 0) {
       return fallback;
     }
     return 30;
   })();
 
   final state = await (db.select(db.syncState)..where((t) => t.id.equals(1))).getSingleOrNull();
   final since = state?.lastServerTs ?? 0;
   final res = await ApiService.I.syncPull(since).timeout(Duration(seconds: pullTimeoutSeconds));
   if (res['success'] != true) return;
   final data = List<Map<String, dynamic>>.from(res['data']['data']);
   
+  int maxTs = since;
   await db.transaction(() async {
-    int maxTs = since;
     for (final it in data) {
       final entity = it['entity'] as String;
       final op = it['op'] as String;
       final serverId = it['server_id'];
       final serverTs = (it['server_ts'] as num).toInt();
       final rawData = it['data'];
       final item = rawData is Map ? Map<String, dynamic>.from(rawData) : <String, dynamic>{};
       
       try {
         await _applyIncoming(entity, op, serverId, serverTs, item);
         if (serverTs > maxTs) maxTs = serverTs;
       } catch (e) {
-        debugPrint('❌ Failed to apply incoming change for $entity: $e');
-        rethrow;
+        debugPrint('❌ Failed to apply incoming change for $entity with server_id $serverId: $e. Skipping item.');
+        // Do not rethrow, to allow other changes in the batch to be processed.
       }
     }
-    
-    final now = Time.nowEpoch();
-    await (db.into(db.syncState)).insertOnConflictUpdate(SyncStateCompanion(
-      id: const d.Value(1),
-      lastServerTs: d.Value(maxTs),
-      lastPullTs: d.Value(now),
-      isSyncing: const d.Value(0),
-    ));
   });
+
+  // Update sync state outside the main transaction to ensure it reflects
+  // the last processed timestamp even if some items failed.
+  final now = Time.nowEpoch();
+  await (db.into(db.syncState)).insertOnConflictUpdate(SyncStateCompanion(
+    id: const d.Value(1),
+    lastServerTs: d.Value(maxTs),
+    lastPullTs: d.Value(now),
+    isSyncing: const d.Value(0),
+  ));
   
   await RoomsRepository(db).refreshAllRoomOccupancy();
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 8</summary>

__

Why: The suggestion correctly identifies a critical issue where a single failed item in a sync batch causes the entire batch to be rolled back, preventing future syncs from succeeding.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Remove invalid mutex result check</summary>

___

**Remove the incorrect check on the return value of <code>_mutex.runExclusive</code>, as it <br>will always be <code>null</code> and cause a false warning to be logged.**

[sync/services/appwrite_sync_manager.dart [179-195]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-c3ce10d1f25a663f4ee7caea430046eb71658a18b63e424392dbff4c67e0ad1cR179-R195)

```diff
-final result = await _mutex.runExclusive(() async {
+await _mutex.runExclusive(() async {
   final existingDoc = await appwriteService.getDocument(
     collectionId: AppwriteConfig.devicesCollectionId,
     documentId: _currentDeviceId!,
   );
   int currentRemoteVersion = 0;
   if (existingDoc != null) {
     currentRemoteVersion = _asInt(existingDoc.data['version'], fallback: 0);
   }
   if (_deviceVersion == null || _deviceVersion! <= currentRemoteVersion) {
     _deviceVersion = currentRemoteVersion + 1;
   }
 });
-if (result == null) {
-  _logger.warning('Failed to acquire mutex for device registration', tag: 'SYNC');
-}
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 5</summary>

__

Why: The suggestion correctly identifies that the return value of `_mutex.runExclusive` is being checked incorrectly, leading to a warning that always fires. Removing the check fixes this logical error.


</details></details></td><td align=center>Low

</td></tr><tr><td rowspan=6>General</td>
<td>



<details><summary>Validate auto-backup time format</summary>

___

**Add validation using <code>int.tryParse</code> when parsing the auto-backup time string from <br><code>SharedPreferences</code> to prevent a <code>FormatException</code> if the data is malformed.**

[sync/services/google_drive_backup_service.dart [1012-1019]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-e3b4109c65d5ed43fec23a4a970f9cd4d76dc29098e91519e42a9ef9b05b57e0R1012-R1019)

```diff
-final timeParts = timeString.split(':');
+final parts = timeString.split(':');
+final hour = (parts.length == 2 && int.tryParse(parts[0]) != null) ? int.parse(parts[0]) : 0;
+final minute = (parts.length == 2 && int.tryParse(parts[1]) != null) ? int.parse(parts[1]) : 0;
 final targetTime = DateTime(
   now.year,
   now.month,
   now.day,
-  int.parse(timeParts[0]),
-  int.parse(timeParts[1]),
+  hour,
+  minute,
 );
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 7</summary>

__

Why: The suggestion correctly identifies a potential crash if the time string from `SharedPreferences` is malformed. Adding validation with `int.tryParse` makes the backup scheduling logic more robust and prevents unhandled exceptions.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Run async operations in parallel</summary>

___

**Improve the performance of the <code>_initialize</code> method by executing independent <br>asynchronous operations in parallel using <code>Future.wait</code> to reduce the total <br>loading time.**

[sync/providers/backup_provider.dart [165-257]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-47584ba29a6614edfca8e7f3bce353f5386008bc53d14cf13ec8c41e14228233R165-R257)

```diff
 Future<void> _initialize() async {
   try {
     final prefs = await SharedPreferences.getInstance();
     final skipPref = prefs.getBool(_driveLoginSkippedKey) ?? false;
-    // جلب آخر وقت نسخ احتياطي (Google Drive)
-    final lastBackup = await _backupService.getLastBackupTime();
+
+    // Run independent async operations in parallel
+    final results = await Future.wait([
+      _backupService.getLastBackupTime(),
+      _localBackupService.getLastLocalBackupTime(),
+      _backupService.estimateDatabaseSize(),
+      _localBackupService.checkPermissions(),
+      _backupService.isAutoBackupEnabled(),
+      _backupService.getAutoBackupFrequency(),
+      _backupService.getAutoBackupTime(),
+      _localBackupService.isAutoLocalBackupEnabled(),
+      _localBackupService.getAutoLocalBackupFrequency(),
+    ]);
+
+    final lastBackup = results[0] as DateTime?;
+    final lastLocalBackup = results[1] as DateTime?;
+    final dbSize = results[2] as int?;
+    final hasPermission = results[3] as bool;
+    final autoEnabled = results[4] as bool;
+    final frequency = results[5] as String;
+    final time = results[6] as String;
+    final enableLocal = results[7] as bool;
+    final localFreq = results[8] as String;
     
-    // جلب آخر وقت نسخ احتياطي محلي
-    final lastLocalBackup = await _localBackupService.getLastLocalBackupTime();
-    
-    // جلب حجم قاعدة البيانات
-    final dbSize = await _backupService.estimateDatabaseSize();
-    
-    // التحقق من أذونات التخزين المحلي
-    final hasPermission = await _localBackupService.checkPermissions();
-    
-    // جلب معلومات مجلد النسخ المحلي
     Map<String, dynamic>? folderInfo;
     List<LocalBackupFile> localBackups = [];
     if (hasPermission) {
-      folderInfo = await _localBackupService.getBackupFolderInfo();
-      localBackups = await _localBackupService.listLocalBackups();
+      final localResults = await Future.wait([
+        _localBackupService.getBackupFolderInfo(),
+        _localBackupService.listLocalBackups(),
+      ]);
+      folderInfo = localResults[0] as Map<String, dynamic>?;
+      localBackups = localResults[1] as List<LocalBackupFile>;
     }
     
-    // جلب إعدادات النسخ التلقائي
-    final autoEnabled = await _backupService.isAutoBackupEnabled();
-    final frequency = await _backupService.getAutoBackupFrequency();
-    final time = await _backupService.getAutoBackupTime();
-    final enableLocal = await _localBackupService.isAutoLocalBackupEnabled();
-    final localFreq = await _localBackupService.getAutoLocalBackupFrequency();
-
     final isAutoEnabled = autoEnabled || enableLocal;
     final resolvedFrequency = autoEnabled ? frequency : localFreq;
     final resolvedBackupType = enableLocal && autoEnabled
         ? BackupType.both
         : enableLocal
             ? BackupType.local
             : autoEnabled
                 ? BackupType.googleDrive
                 : BackupType.both;
 
-    // محاولة استعادة جلسة Google Drive تلقائياً
     await _backupService.attemptSilentSignIn();
     
-    // التحقق من تسجيل الدخول في Google Drive
     GoogleSignInAccount? account;
     List<DriveBackupFile> driveBackups = [];
     if (_backupService.isSignedIn) {
       account = _backupService.currentUser;
-      
-      // إشعار مديري المزامنة بنجاح تسجيل الدخول الصامت
       await _notifySyncManagers(true);
-      
-      // جلب قائمة النسخ المتاحة في Google Drive
       try {
         driveBackups = await _backupService.listBackupFiles();
       } catch (e) {
         debugPrint('⚠️ خطأ في جلب نسخ Google Drive: $e');
       }
     }
 
     if (account != null && skipPref) {
       await prefs.setBool(_driveLoginSkippedKey, false);
     }
     final driveLoginSkipped = account == null && skipPref;
 
     state = state.copyWith(
       lastBackupTime: lastBackup,
       lastLocalBackupTime: lastLocalBackup,
       databaseSizeBytes: dbSize,
       hasStoragePermission: hasPermission,
       backupFolderInfo: folderInfo,
       localBackups: localBackups,
       availableBackups: driveBackups,
       signedInAccount: account,
       autoSettings: AutoBackupSettings(
         isEnabled: isAutoEnabled,
         frequency: resolvedFrequency,
         time: time,
         enableLocalBackup: enableLocal,
         enableGoogleDriveBackup: autoEnabled,
         backupType: resolvedBackupType,
       ),
       driveLoginSkipped: driveLoginSkipped,
     );
   } catch (e) {
     debugPrint('❌ خطأ في تهيئة BackupStatusNotifier: $e');
     state = state.copyWith(
       status: BackupStatus.error,
       message: 'خطأ في التهيئة: ${e.toString()}',
     );
   }
 }
```


- [ ] **Apply / Chat** <!-- /improve --apply_suggestion=9 -->


<details><summary>Suggestion importance[1-10]: 7</summary>

__

Why: The suggestion correctly points out a performance bottleneck and proposes a valid solution using `Future.wait` to run independent async calls in parallel, which will improve the initialization speed.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Move file I/O out of DB transaction</summary>

___

**In <code>createLocalSnapshot</code>, move file I/O operations outside the <code>db.transaction</code> <br>block to improve performance and reduce the duration of database locks.**

[sync/services/restore_fix_service.dart [109-159]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-f56939d65665eb4b5391d1091411db16fbcbc9b6510fad80e90101172a763cf4R109-R159)

```diff
 Future<RestoreSnapshot> createLocalSnapshot(String prefix) async {
-  return await db.transaction<RestoreSnapshot>(() async {
-    // ...
-    final jsonString = jsonEncode(snapshotData);
-    final file = File(filePath);
-    await file.writeAsString(jsonString);
-    // ...
-    return RestoreSnapshot(
-      filePath: filePath,
-      createdAt: DateTime.now(),
-      recordCounts: recordCounts,
-      totalSizeBytes: await file.length(),
-    );
+  debugPrint('📸 إنشاء لقطة احتياطية أمان...');
+  // 1. Export data and counts inside a short transaction
+  final exportResult = await db.transaction<Map<String, dynamic>>(() async {
+    final bookingsData = await bookingsDao.exportToJson();
+    final roomsData = await roomsDao.exportToJson();
+    final paymentsData = await paymentsDao.exportToJson();
+    final debtsData = await debtsDao.exportToJson(includeDeleted: true);
+    return {
+      'data': {
+        'bookings': bookingsData,
+        'rooms': roomsData,
+        'payments': paymentsData,
+        'debts': debtsData,
+      },
+      'counts': {
+        'bookings': bookingsData.length,
+        'rooms': roomsData.length,
+        'payments': paymentsData.length,
+        'debts': debtsData.length,
+      }
+    };
   });
+  // 2. Perform file I/O outside the transaction
+  final directory = await getTemporaryDirectory();
+  if (!await directory.exists()) await directory.create(recursive: true);
+  final timestamp = Time.nowEpoch();
+  final filename = '${prefix}_restore_snapshot_$timestamp.json';
+  final filePath = '${directory.path}/$filename';
+  final jsonString = jsonEncode({
+    'metadata': {
+      'timestamp': timestamp,
+      'createdAt': DateTime.now().toIso8601String(),
+      'deviceId': 'local_device',
+      'version': '1.0',
+    },
+    ...exportResult['data'] as Map<String, dynamic>,
+  });
+  final file = File(filePath);
+  await file.writeAsString(jsonString);
+  final totalSizeBytes = await file.length();
+  return RestoreSnapshot(
+    filePath: filePath,
+    createdAt: DateTime.now(),
+    recordCounts: exportResult['counts'] as Map<String, int>,
+    totalSizeBytes: totalSizeBytes,
+  );
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 7</summary>

__

Why: The suggestion correctly points out that performing file I/O within a database transaction is a bad practice that can lead to performance issues by holding locks for too long.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Store conflict timestamp consistently</summary>

___

**In <code>_logConflict</code>, replace the complex timestamp normalization logic with <br><code>DateTime.now().millisecondsSinceEpoch</code> to ensure the <code>occurred_at</code> value is stored <br>consistently.**

[sync/services/restore_fix_service.dart [929-952]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-f56939d65665eb4b5391d1091411db16fbcbc9b6510fad80e90101172a763cf4R929-R952)

```diff
-Future<void> _logConflict({
-  required String fixId,
-  required String tableName,
-  required String fixType,
-  String? localUuid,
-  required Map<String, dynamic> oldData,
-  required Map<String, dynamic> newData,
-}) async {
-  await _ensureConflictLogTable();
-  final epoch = Time.nowEpoch();
-  final normalized = epoch < 1000000000000 ? epoch * 1000 : epoch;
-  await db.customStatement(
-    'INSERT INTO restore_conflict_log (fix_id, fix_type, table_name, local_uuid, old_data, new_data, occurred_at) VALUES (?, ?, ?, ?, ?, ?, ?)',
-    [
-      fixId,
-      fixType,
-      tableName,
-      localUuid,
-      jsonEncode(oldData),
-      jsonEncode(newData),
-      normalized,
-    ],
-  );
-}
+await _ensureConflictLogTable();
+final occurredAt = DateTime.now().millisecondsSinceEpoch;
+await db.customStatement(
+  'INSERT INTO restore_conflict_log (fix_id, fix_type, table_name, local_uuid, old_data, new_data, occurred_at) VALUES (?, ?, ?, ?, ?, ?, ?)',
+  [
+    fixId,
+    fixType,
+    tableName,
+    localUuid,
+    jsonEncode(oldData),
+    jsonEncode(newData),
+    occurredAt,
+  ],
+);
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 6</summary>

__

Why: The suggestion correctly identifies that the timestamp normalization logic is confusing and potentially buggy, proposing a simpler and more robust way to store a consistent timestamp.


</details></details></td><td align=center>Low

</td></tr><tr><td>



<details><summary>Always show dismiss button</summary>

___

**Improve user experience by always showing the dismiss button on status messages, <br>including errors, by removing the <code>if (state.status != BackupStatus.error)</code> <br>condition.**

[sync/screens/comprehensive_backup_screen.dart [144-148]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-0127e1a8255c172204cf53ef27653e6665e42131863bce402e86e6fead2ab616R144-R148)

```diff
-if (state.status != BackupStatus.error)
-  IconButton(
-    onPressed: () => ref.read(backupStatusProvider.notifier).clearMessage(),
-    icon: Icon(Icons.close, color: color, size: 20),
-  ),
+IconButton(
+  onPressed: () => ref.read(backupStatusProvider.notifier).clearMessage(),
+  icon: Icon(Icons.close, color: color, size: 20),
+),
```


- [ ] **Apply / Chat** <!-- /improve --apply_suggestion=12 -->


<details><summary>Suggestion importance[1-10]: 5</summary>

__

Why: This is a good usability improvement, as it allows the user to dismiss persistent error messages from the UI, which is currently not possible.


</details></details></td><td align=center>Low

</td></tr><tr><td>



<details><summary>Simplify network connectivity check</summary>

___

**Simplify the network connectivity check by removing the unnecessary branch that <br>handles a <code>List<ConnectivityResult></code>, as <code>checkConnectivity()</code> returns a single value.**

[sync/services/appwrite_sync_manager.dart [378-384]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-c3ce10d1f25a663f4ee7caea430046eb71658a18b63e424392dbff4c67e0ad1cR378-R384)

```diff
 final connectivityResult = await Connectivity().checkConnectivity();
-final hasConnection = connectivityResult is List<ConnectivityResult>
-    ? connectivityResult.any((r) => r != ConnectivityResult.none)
-    : connectivityResult != ConnectivityResult.none;
+final hasConnection = connectivityResult != ConnectivityResult.none;
 if (!hasConnection) {
   throw Exception('No internet connection');
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 4</summary>

__

Why: The suggestion correctly points out that the check for `List<ConnectivityResult>` is redundant based on the current `connectivity_plus` package behavior, and simplifying it improves code clarity and removes dead code.


</details></details></td><td align=center>Low

</td></tr><tr><td rowspan=8>Possible issue</td>
<td>



<details><summary>Prevent concurrent sync operations</summary>

___

**Refactor <code>pushLocalChanges()</code> and <code>pullRemoteChanges()</code> to use the main <code>sync()</code> <br>method with appropriate flags. This ensures all synchronization operations are <br>protected by the mutex, preventing race conditions and centralizing sync logic.**

[sync/services/appwrite_sync_manager.dart [1469-1564]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-c3ce10d1f25a663f4ee7caea430046eb71658a18b63e424392dbff4c67e0ad1cR1469-R1564)

```diff
 Future<bool> pushLocalChanges() async {
-  // انتظر إذا كانت المزامنة جارية بدلاً من التخطي
-  int retries = 0;
-  while (_currentStatus == SyncStatus.syncing && retries < 10) {
-    await Future.delayed(SyncConstants.shortPollingDelay);
-    retries++;
-  }
-  
-  if (_currentStatus == SyncStatus.syncing) {
-    _logger.warning('تخطي الرفع - المزامنة جارية لفترة طويلة', tag: 'SYNC');
-    return false;
-  }
-
-  try {
-    _logger.info('📤 رفع التغييرات المحلية إلى Appwrite...', tag: 'SYNC');
-    
-    final pushedCount = await _pushAllEntities();
-    
-    _lastSyncTime = DateTime.now();
-    await _saveSettings();
-    
-    _logger.info('✅ تم رفع $pushedCount تغيير إلى Appwrite', tag: 'SYNC');
-    return true;
-  } catch (e, stackTrace) {
-    _logger.error('❌ خطأ في رفع التغييرات إلى Appwrite', 
-      error: e, 
-      stackTrace: stackTrace, 
-      tag: 'SYNC'
-    );
-    return false;
-  }
+  final result = await sync(push: true, pull: false);
+  return result.status == SyncStatus.success;
 }
 
 Future<bool> pullRemoteChanges() async {
-  if (_currentStatus == SyncStatus.syncing) {
-    _logger.warning('⏸️ تخطي السحب - المزامنة جارية', tag: 'SYNC');
-    return false;
-  }
-
-  try {
-    _logger.info('📥 سحب التغييرات من Appwrite...', tag: 'SYNC');
-    
-    int recordsPulled = 0;
-    
-    // مزامنة الغرف
-    final rooms = await appwriteService.listRooms(useCache: false);
-    final roomsSynced = await _syncRooms(rooms);
-    recordsPulled += roomsSynced;
-
-    // مزامنة الحجوزات
-    final bookings = await appwriteService.listBookings(useCache: false);
-    final bookingsSynced = await _syncBookings(bookings);
-    recordsPulled += bookingsSynced;
-
-    // مزامنة الموظفين
-    final employees = await appwriteService.listEmployees(useCache: false);
-    final employeesSynced = await _syncEmployees(employees);
-    recordsPulled += employeesSynced;
-
-    // مزامنة المصروفات
-    final expenses = await appwriteService.listExpenses(useCache: false);
-    final expensesSynced = await _syncExpenses(expenses);
-    recordsPulled += expensesSynced;
-
-    // مزامنة المدفوعات
-    final payments = await appwriteService.listPayments(useCache: false);
-    final paymentsSynced = await _syncPayments(payments);
-    recordsPulled += paymentsSynced;
-
-    // مزامنة الديون
-    final debts = await appwriteService.listDebts(useCache: false);
-    final debtsSynced = await _syncDebts(debts);
-    recordsPulled += debtsSynced;
-    
-    _lastSyncTime = DateTime.now();
-    await _saveSettings();
-    
-    if (recordsPulled > 0) {
-      _logger.info('✅ تم سحب $recordsPulled سجل من Appwrite', tag: 'SYNC');
-      return true;
-    } else {
-      _logger.info('ℹ️ لا توجد تغييرات جديدة من Appwrite', tag: 'SYNC');
-      return false;
-    }
-    
-  } catch (e, stackTrace) {
-    _logger.error('❌ خطأ في سحب التغييرات من Appwrite', 
-      error: e, 
-      stackTrace: stackTrace, 
-      tag: 'SYNC'
-    );
-    return false;
-  }
+  final result = await sync(push: false, pull: true);
+  return result.status == SyncStatus.success && result.recordsPulled > 0;
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: The suggestion correctly identifies a critical race condition where `pushLocalChanges` and `pullRemoteChanges` bypass the mutex, which could lead to data corruption, and provides a robust solution.


</details></details></td><td align=center>High

</td></tr><tr><td>



<details><summary>Allow nullable fields to clear</summary>

___

**Modify the <code>copyWith</code> method in <code>BackupState</code> to allow nullable fields to be set to <br><code>null</code>. This fixes bugs where state, such as the signed-in account or status <br>messages, cannot be cleared.**

[sync/providers/backup_provider.dart [78-143]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-47584ba29a6614edfca8e7f3bce353f5386008bc53d14cf13ec8c41e14228233R78-R143)

```diff
 class BackupState {
+  static const Object _unset = Object();
+
   final BackupStatus status;
   final String? message;
   final double? progress;
   final GoogleSignInAccount? signedInAccount;
   final DateTime? lastBackupTime;
   final List<DriveBackupFile> availableBackups;
   final List<LocalBackupFile> localBackups;
   final DateTime? lastLocalBackupTime;
   final AutoBackupSettings autoSettings;
   final int? databaseSizeBytes;
   final bool hasStoragePermission;
   final Map<String, dynamic>? backupFolderInfo;
   final String? lastSqliteBackupPath;
   final bool driveLoginSkipped;
 
   BackupState({
     this.status = BackupStatus.idle,
     this.message,
     this.progress,
     this.signedInAccount,
     this.lastBackupTime,
     this.availableBackups = const [],
     this.localBackups = const [],
     this.lastLocalBackupTime,
     this.autoSettings = const AutoBackupSettings(),
     this.databaseSizeBytes,
     this.hasStoragePermission = false,
     this.backupFolderInfo,
     this.lastSqliteBackupPath,
     this.driveLoginSkipped = false,
   });
 
   BackupState copyWith({
     BackupStatus? status,
-    String? message,
-    double? progress,
-    GoogleSignInAccount? signedInAccount,
-    DateTime? lastBackupTime,
+    Object? message = _unset,
+    Object? progress = _unset,
+    Object? signedInAccount = _unset,
+    Object? lastBackupTime = _unset,
     List<DriveBackupFile>? availableBackups,
     List<LocalBackupFile>? localBackups,
-    DateTime? lastLocalBackupTime,
+    Object? lastLocalBackupTime = _unset,
     AutoBackupSettings? autoSettings,
-    int? databaseSizeBytes,
+    Object? databaseSizeBytes = _unset,
     bool? hasStoragePermission,
-    Map<String, dynamic>? backupFolderInfo,
-    String? lastSqliteBackupPath,
+    Object? backupFolderInfo = _unset,
+    Object? lastSqliteBackupPath = _unset,
     bool? driveLoginSkipped,
   }) {
     return BackupState(
       status: status ?? this.status,
-      message: message ?? this.message,
-      progress: progress ?? this.progress,
-      signedInAccount: signedInAccount ?? this.signedInAccount,
-      lastBackupTime: lastBackupTime ?? this.lastBackupTime,
+      message: message == _unset ? this.message : message as String?,
+      progress: progress == _unset ? this.progress : progress as double?,
+      signedInAccount: signedInAccount == _unset ? this.signedInAccount : signedInAccount as GoogleSignInAccount?,
+      lastBackupTime: lastBackupTime == _unset ? this.lastBackupTime : lastBackupTime as DateTime?,
       availableBackups: availableBackups ?? this.availableBackups,
       localBackups: localBackups ?? this.localBackups,
-      lastLocalBackupTime: lastLocalBackupTime ?? this.lastLocalBackupTime,
+      lastLocalBackupTime: lastLocalBackupTime == _unset ? this.lastLocalBackupTime : lastLocalBackupTime as DateTime?,
       autoSettings: autoSettings ?? this.autoSettings,
-      databaseSizeBytes: databaseSizeBytes ?? this.databaseSizeBytes,
+      databaseSizeBytes: databaseSizeBytes == _unset ? this.databaseSizeBytes : databaseSizeBytes as int?,
       hasStoragePermission: hasStoragePermission ?? this.hasStoragePermission,
-      backupFolderInfo: backupFolderInfo ?? this.backupFolderInfo,
-      lastSqliteBackupPath: lastSqliteBackupPath ?? this.lastSqliteBackupPath,
+      backupFolderInfo: backupFolderInfo == _unset ? this.backupFolderInfo : backupFolderInfo as Map<String, dynamic>?,
+      lastSqliteBackupPath: lastSqliteBackupPath == _unset ? this.lastSqliteBackupPath : lastSqliteBackupPath as String?,
       driveLoginSkipped: driveLoginSkipped ?? this.driveLoginSkipped,
     );
   }
 }
```


- [ ] **Apply / Chat** <!-- /improve --apply_suggestion=15 -->


<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: The suggestion correctly identifies a critical bug in the `copyWith` implementation where nullable fields cannot be set to `null`, breaking functionality like `signOut()` and `clearMessage()`, and proposes a standard and effective solution.


</details></details></td><td align=center>High

</td></tr><tr><td>



<details><summary>Fix timestamp unit mismatch</summary>

___

**Fix a timestamp unit mismatch in the delta pull logic by converting milliseconds <br>to seconds before comparison. This prevents the incorrect reprocessing of <br>already synced files.**

[sync/services/google_drive_delta_sync.dart [150-165]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-616099eece02f842e140067261394d40037c8aa62ed655803c14a98f32278929R150-R165)

```diff
 for (final file in deltaFiles) {
-  if (file.createdTime.millisecondsSinceEpoch <= lastPullTs) continue;
+  final createdSeconds = file.createdTime.millisecondsSinceEpoch ~/ 1000;
+  if (createdSeconds <= lastPullTs) continue;
   
   final sourceDeviceId = file.appProperties['device_id'];
   if (sourceDeviceId == _deviceId) continue;
 
   final deltaData = await _downloadDeltaFile(file.fileId);
   if (deltaData != null) {
     final changes = await _applyDeltaChanges(deltaData);
     appliedChanges += changes;
   }
 }
 
 if (appliedChanges > 0) {
   await _updateLastPullTimestamp();
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 9</summary>

__

Why: The suggestion correctly identifies a critical bug where timestamps in milliseconds are compared to timestamps in seconds, which breaks the delta pull logic and causes all files to be re-processed on every pull.


</details></details></td><td align=center>High

</td></tr><tr><td>



<details><summary>Enable clearing schedule fields</summary>

___

**Update the <code>AutoBackupSettings.copyWith</code> method to allow nullable fields like <br><code>weekday</code> and <code>day</code> to be set to <code>null</code>. This prevents stale scheduling data from <br>persisting when the backup frequency is changed.**

[sync/providers/backup_provider.dart [29-75]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-47584ba29a6614edfca8e7f3bce353f5386008bc53d14cf13ec8c41e14228233R29-R75)

```diff
 class AutoBackupSettings {
+  static const Object _unset = Object();
+
   final bool isEnabled;
   final String frequency; // daily, weekly, monthly
   final String time; // HH:mm format
   final int? weekday; // 1-7 للنسخ الأسبوعي
   final int? day; // 1-31 للنسخ الشهري
   final BackupType backupType; // نوع النسخ (Google Drive، محلي، أو كليهما)
   final bool enableLocalBackup; // تفعيل النسخ المحلي
   final bool enableGoogleDriveBackup; // تفعيل النسخ السحابي
   final BackupFormat backupFormat; // تنسيق النسخ الاحتياطي المستخدم
 
   const AutoBackupSettings({
     this.isEnabled = true,
     this.frequency = 'daily',
     this.time = '02:00',
     this.weekday,
     this.day,
     this.backupType = BackupType.both,
     this.enableLocalBackup = true,
     this.enableGoogleDriveBackup = true,
     this.backupFormat = BackupFormat.json,
   });
 
   AutoBackupSettings copyWith({
     bool? isEnabled,
     String? frequency,
     String? time,
-    int? weekday,
-    int? day,
+    Object? weekday = _unset,
+    Object? day = _unset,
     BackupType? backupType,
     bool? enableLocalBackup,
     bool? enableGoogleDriveBackup,
     BackupFormat? backupFormat,
   }) {
     return AutoBackupSettings(
       isEnabled: isEnabled ?? this.isEnabled,
       frequency: frequency ?? this.frequency,
       time: time ?? this.time,
-      weekday: weekday ?? this.weekday,
-      day: day ?? this.day,
+      weekday: weekday == _unset ? this.weekday : weekday as int?,
+      day: day == _unset ? this.day : day as int?,
       backupType: backupType ?? this.backupType,
       enableLocalBackup: enableLocalBackup ?? this.enableLocalBackup,
       enableGoogleDriveBackup: enableGoogleDriveBackup ?? this.enableGoogleDriveBackup,
       backupFormat: backupFormat ?? this.backupFormat,
     );
   }
 }
```


- [ ] **Apply / Chat** <!-- /improve --apply_suggestion=17 -->


<details><summary>Suggestion importance[1-10]: 8</summary>

__

Why: The suggestion correctly identifies a flaw in the `AutoBackupSettings.copyWith` method that leads to inconsistent state management by preventing nullable fields like `weekday` and `day` from being cleared, which could cause future scheduling bugs.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Ensure foreign keys restored</summary>

___

**To prevent foreign key constraint errors during snapshot restoration, <br>temporarily disable <code>PRAGMA foreign_keys</code> before the database transaction and <br>ensure it is re-enabled in a <code>finally</code> block.**

[sync/services/restore_fix_service.dart [965-989]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-f56939d65665eb4b5391d1091411db16fbcbc9b6510fad80e90101172a763cf4R965-R989)

```diff
-await db.transaction(() async {
-  // مسح الجداول المتأثرة
-  await db.delete(db.bookings).go();
-  await db.delete(db.rooms).go();
-  await db.delete(db.payments).go();
-  await db.delete(db.debts).go();
-  
-  // استعادة البيانات
-  if (snapshotData.containsKey('bookings')) {
-    await bookingsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['bookings']), clearExisting: false);
-  }
-  if (snapshotData.containsKey('rooms')) {
-    await roomsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['rooms']), clearExisting: false);
-  }
-  if (snapshotData.containsKey('payments')) {
-    await paymentsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['payments']), clearExisting: false);
-  }
-  if (snapshotData.containsKey('debts')) {
-    await debtsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['debts']), clearExisting: false);
-  }
+await db.customStatement('PRAGMA foreign_keys = OFF');
+try {
+  await db.transaction(() async {
+    // مسح الجداول المتأثرة
+    await db.delete(db.bookings).go();
+    await db.delete(db.rooms).go();
+    await db.delete(db.payments).go();
+    await db.delete(db.debts).go();
+    
+    // استعادة البيانات
+    if (snapshotData.containsKey('rooms')) {
+      await roomsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['rooms']), clearExisting: false);
+    }
+    if (snapshotData.containsKey('bookings')) {
+      await bookingsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['bookings']), clearExisting: false);
+    }
+    if (snapshotData.containsKey('payments')) {
+      await paymentsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['payments']), clearExisting: false);
+    }
+    if (snapshotData.containsKey('debts')) {
+      await debtsDao.importFromJson(List<Map<String, dynamic>>.from(snapshotData['debts']), clearExisting: false);
+    }
 
-  await db.delete(db.bookingNights).go();
-  await db.delete(db.hotelDayLedger).go();
-  await _rebuildBookingStructures(DateTime.now());
-});
+    await db.delete(db.bookingNights).go();
+    await db.delete(db.hotelDayLedger).go();
+    await _rebuildBookingStructures(DateTime.now());
+  });
+} finally {
+  await db.customStatement('PRAGMA foreign_keys = ON');
+}
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 8</summary>

__

Why: The suggestion correctly identifies a potential foreign key constraint violation during the snapshot restore process and proposes a robust solution by temporarily disabling and then re-enabling foreign key checks, which significantly improves the reliability of the restore operation.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Avoid crash on size parsing</summary>

___

**Replace <code>int.parse</code> with <code>int.tryParse</code> when parsing the <code>file.size</code> from the Google <br>Drive API. This prevents a potential <code>FormatException</code> and application crash if <br>the API returns a non-numeric value.**

[sync/services/google_drive_backup_service.dart [45-53]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-e3b4109c65d5ed43fec23a4a970f9cd4d76dc29098e91519e42a9ef9b05b57e0R45-R53)

```diff
 factory DriveBackupFile.fromDriveFile(drive.File file) {
   return DriveBackupFile(
     fileId: file.id!,
     fileName: file.name!,
     createdTime: file.createdTime!,
-    size: file.size != null ? int.parse(file.size!) : null,
+    size: file.size != null ? int.tryParse(file.size!) : null,
     metadata: file.appProperties,
   );
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 7</summary>

__

Why: This is a valid improvement for robustness, as using `int.parse` on external API data is unsafe and could lead to a runtime crash, which `int.tryParse` correctly prevents.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Guard against malformed time values</summary>

___

**Add validation and use <code>int.tryParse</code> when parsing the <code>auto_backup_time</code> string <br>from <code>SharedPreferences</code>. This prevents a potential crash during settings <br>restoration if the time format is invalid.**

[sync/services/google_drive_backup_service.dart [949-961]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-e3b4109c65d5ed43fec23a4a970f9cd4d76dc29098e91519e42a9ef9b05b57e0R949-R961)

```diff
 final timeStr = prefs.getString('auto_backup_time') ?? '21:00';
 final parts = timeStr.split(':');
-final hour = int.parse(parts[0]);
-final minute = int.parse(parts[1]);
+final hour = (parts.length == 2 && int.tryParse(parts[0]) != null) ? int.parse(parts[0]) : 21;
+final minute = (parts.length == 2 && int.tryParse(parts[1]) != null) ? int.parse(parts[1]) : 0;
 final scheduledEnabled = prefs.getBool('scheduled_backup_enabled') ?? true;
 
 if (scheduledEnabled) {
   await AlarmBackup.rescheduleDaily(hour, minute);
-  await AutoBackupTask.scheduleDaily(time: timeStr);
+  await AutoBackupTask.scheduleDaily(time: '$hour:${minute.toString().padLeft(2, '0')}');
 } else {
   await AlarmBackup.cancelAlarm();
   await AutoBackupTask.cancelScheduled();
 }
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 7</summary>

__

Why: The suggestion correctly identifies that parsing a string from `SharedPreferences` without validation can cause a crash, and the proposed change makes the code more robust against corrupt or invalid data.


</details></details></td><td align=center>Medium

</td></tr><tr><td>



<details><summary>Guard against invalid payloads</summary>

___

**Prevent a potential crash during sync by safely casting <code>data['room_number']</code> <br>instead of using a direct cast. This makes the pull operation more robust <br>against malformed data from the server.**

[sync/services/sync_service.dart [268-302]](https://github.com/NassarAlshabi1/marina-hotel-wit-app/pull/298/files#diff-75d88958e817f8bd130124b2d5ff2bafaf53904f49b61d500bfe0450e512a034R268-R302)

```diff
 case 'rooms':
-  final rn = data['room_number'] as String;
+  final rnValue = data['room_number'];
+  final rn = rnValue is String ? rnValue : null;
+  if (rn == null || rn.isEmpty) {
+    debugPrint('⚠️ تخطي rooms change لعدم وجود room_number صالح (server_id=$serverId)');
+    break;
+  }
   final local = await (db.select(db.rooms)..where((t) => t.roomNumber.equals(rn))).getSingleOrNull();
   if (local != null) {
     if (serverTs >= local.lastModified) {
       await roomsDao.updateByNumber(
         rn,
         RoomsCompanion(
           type: data['type'] != null ? d.Value(data['type']) : const d.Value.absent(),
           price: data['price'] != null ? d.Value((data['price'] as num).toDouble()) : const d.Value.absent(),
           status: data['status'] != null ? d.Value(data['status']) : const d.Value.absent(),
           imageUrl: data['image_url'] != null ? d.Value(data['image_url']) : const d.Value.absent(),
           serverId: d.Value(serverId is int ? serverId : null),
           origin: const d.Value('server'),
         ),
         originIsServer: true,
       );
     }
   } else {
     await roomsDao.insertOne(
       RoomsCompanion(
         roomNumber: d.Value(rn),
         type: d.Value(data['type'] ?? ''),
         price: d.Value((data['price'] as num?)?.toDouble() ?? 0),
         status: d.Value(data['status'] ?? 'شاغرة'),
         imageUrl: d.Value(data['image_url']),
         serverId: d.Value(serverId is int ? serverId : null),
       ),
       originIsServer: true,
     );
   }
   if (op == 'delete' || data['deleted_at'] != null) {
     await roomsDao.softDelete(rn, originIsServer: true);
   }
   break;
```



`[To ensure code accuracy, apply this suggestion manually]`


<details><summary>Suggestion importance[1-10]: 7</summary>

__

Why: The suggestion correctly points out that a direct cast on `data['room_number']` could crash the entire sync process if the payload is malformed. Adding a type check makes the data processing more robust and prevents a single bad record from failing the whole batch.


</details></details></td><td align=center>Medium

</td></tr>
<tr><td align="center" colspan="2">

- [ ] More <!-- /improve --more_suggestions=true -->

</td><td></td></tr></tbody></table>


